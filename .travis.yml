language:
  - python
install:
  - pip install -r requirements.txt
  - pip install -r requirements-dev.txt
env:
  global:
    - SERMIN_TEST_MODE=full
script:
  - flake8 && sudo /home/travis/virtualenv/python2.7/bin/python setup.py nosetests -s -vv
